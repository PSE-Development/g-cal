package com.gcal.ui.screens.profile.settings

/**
 * AppLanguage — Enum representing the supported application languages.
 *
 */
enum class AppLanguage(val displayName: String, val code: String) {
    GERMAN("Deutsch", "de"),
    ENGLISH("English", "en")
}